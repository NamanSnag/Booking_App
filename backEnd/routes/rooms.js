// import express from 'express';
// const roomRouter = express.Router();
// import room from '../controllers/room_Controller.js';
// import { verifyAdmin } from '../utils/jwt_Verify.js';

// // add hotel routes
// roomRouter.post('/:hotelId', verifyAdmin, room.addRoom);

// // Update hotel routes
// roomRouter.put('/:id', verifyAdmin, room.updateRoom);

// // delete hotel routes
// roomRouter.delete('/:id/:hotelId', verifyAdmin, room.deleteRoom);

// // get hotel routes
// roomRouter.get('/:id', room.getRoom);

// // get all hotels routes
// roomRouter.get('/', room.allRoom);

// export default roomRouter;